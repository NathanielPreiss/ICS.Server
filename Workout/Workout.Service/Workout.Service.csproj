<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<AssemblyName>ICS.Workout.Service</AssemblyName>
		<RootNamespace>ICS.Workout</RootNamespace>
		<TargetFramework>net6.0</TargetFramework>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>
	</PropertyGroup>

	<ItemGroup>
		<ProjectReference Include="..\Workout.Infrastructure\Workout.Infrastructure.csproj" />
	</ItemGroup>

	<ItemGroup>
		<Using Include="ICS.Exercise" />
		<Using Include="Microsoft.EntityFrameworkCore" />
		<Using Include="Microsoft.Extensions.Logging" />
		<Using Include="NServiceBus" />
	</ItemGroup>

</Project>